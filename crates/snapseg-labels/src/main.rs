//! `snapseg-labels` CLI binary placeholder.
//!
//! The real command surface (notably the `coco` subcommand that rolls a
//! `labels/` directory into a single COCO JSON for handoff to a training
//! pipeline) lands in M2-T06. Today this binary only exists so the crate
//! builds with its declared `[[bin]]` entry.

fn main() {
    eprintln!("snapseg-labels CLI: TODO M2-T06");
}
